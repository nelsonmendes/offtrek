<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="offtrek.mobile.app.MainActivity$PlaceholderFragment">

    <TextView
        android:id="@+id/section_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <FrameLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true">

        <Button
            android:layout_width="300dp"
            android:layout_height="50dp"
            android:text="@string/GPS_Capture_Start"
            android:id="@id/startButton"
            android:layout_gravity="center_horizontal|top"
            android:onClick="start_GPS_Capture" />

        <Button
            android:layout_width="300dp"
            android:layout_height="50dp"
            android:text="@string/GPS_Capture_Stop"
            android:id="@id/stopButton"
            android:layout_gravity="center_horizontal|bottom"
            android:onClick="stop_GPS_Capture" />

        <Button
            android:layout_width="209dp"
            android:layout_height="94dp"
            android:text="@string/SendRequest"
            android:id="@id/httpRequestButton"
            android:layout_gravity="center"
            android:onClick="send_HTTP_Request"/>
    </FrameLayout>


</RelativeLayout>
